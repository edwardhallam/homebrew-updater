# ============================================================================
# NOTIFICATION CONFIGURATION
# ============================================================================

# Notification Platform: Choose which webhook service(s) to use
# Options: "discord", "slack", or "both"
# Default: "discord" (for backwards compatibility)
NOTIFICATION_PLATFORM=discord

# ----------------------------------------------------------------------------
# Discord Webhook Configuration (if using Discord)
# ----------------------------------------------------------------------------
# Create a webhook in your Discord server: Server Settings → Integrations → Webhooks
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Discord User ID for @mentions
# Get your user ID: Enable Developer Mode → Right-click username → Copy User ID
DISCORD_USER_ID=YOUR_USER_ID_HERE

# ----------------------------------------------------------------------------
# Slack Webhook Configuration (if using Slack)
# ----------------------------------------------------------------------------
# Create a Slack incoming webhook: https://api.slack.com/messaging/webhooks
# Your Slack App → Incoming Webhooks → Add New Webhook to Workspace
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Homebrew Configuration
# Path to Homebrew binary (default for Apple Silicon Macs)
BREW_PATH=/opt/homebrew/bin/brew

# Idle threshold in seconds (default: 300 = 5 minutes)
# If user is idle beyond this threshold, sudo operations will be skipped
IDLE_THRESHOLD_SECONDS=300

# Log retention (number of log files to keep)
MAX_LOG_FILES=10
